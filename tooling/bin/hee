#!/bin/sh
# hee: tiny POSIX router entrypoint (bootstrap stool)
# Later: replaced by cmd/hee (go/rust) with identical UX.

set -u

usage() {
  cat <<'TXT'
hee — tiny router (POSIX sh)

usage:
  hee lint [--mode warn|error]
  hee hooks install [--repo <path>]
  hee help

notes:
  - default lint mode is warn
  - hooks install is optional; it installs pre-commit symlink
TXT
}

# resolve repo root if possible (best-effort)
REPO_ROOT="$(git rev-parse --show-toplevel 2>/dev/null || true)"
if [ -z "${REPO_ROOT}" ]; then
  REPO_ROOT="."
fi

cmd="${1:-help}"
shift 2>/dev/null || true

case "${cmd}" in
  lint)
    # pass-through
    exec "${REPO_ROOT}/tooling/bin/hee-lint" "$@"
    ;;
  hooks)
    sub="${1:-help}"
    shift 2>/dev/null || true
    case "${sub}" in
      install)
        exec "${REPO_ROOT}/tooling/bin/hee-hooks-install" "$@"
        ;;
      *)
        usage
        exit 0
        ;;
    esac
    ;;
  help|--help|-h|"")
    usage
    exit 0
    ;;
  *)
    echo "🟡 🟦 b # WARN: unknown command: ${cmd}" 1>&2
    usage
    exit 0
    ;;
esac
