# Prompting Rules

## Pager Prevention

All commands must disable pagers (e.g., `GIT_PAGER=cat`, `--no-pager`) to avoid blocking automation.

This section documents pager prevention as required by CI.
