name: hee-preflight
on:
  pull_request:
  push:
    branches:
      - main
jobs:
  hee-preflight:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Ensure local main exists
        run: git fetch origin main:main --force
      - name: HEE preflight
        run: ci/git/hee-preflight.sh
